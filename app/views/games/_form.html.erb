<%= simple_form_for(@game, html: {class: "form-horizontal"}) do |f| %>
  <%= f.input :team_one_id, collection: Team.all, label_method: :name %>
  <%= f.input :team_two_id, collection: Team.all, label_method: :name %>
  <%= f.input :date %>
  <div class="form-actions">
    <%= f.button :submit, class: "btn btn-success" %>
  </div>

<% end %>
